(()=>{'use strict';
const $=s=>document.querySelector(s);let raf=0;
const REST=new Set(['RH','RTT','RC','AA','MA','CA','RF','RTA','RTTA','SYR','OFF','REPOS','-','']);
const HOURS={M:7.833,J:7.833,J4:7.833,S:7.5,N:9.833};
function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function iso(d=new Date()){const p=new Intl.DateTimeFormat('en-CA',{year:'numeric',month:'2-digit',day:'2-digit',timeZone:'Europe/Paris'}).formatToParts(d),g=t=>p.find(x=>x.type===t)?.value||'';return`${g('year')}-${g('month')}-${g('day')}`}
function dObj(s){return new Date(String(s).slice(0,10)+'T12:00:00')}
function fmtDate(s,short=false){const d=dObj(s);return d.toLocaleDateString('fr-FR',short?{weekday:'short',day:'numeric',month:'short'}:{weekday:'long',day:'numeric',month:'long'}).replace(/^./,c=>c.toUpperCase())}
function monthName(){return dObj(iso()).toLocaleDateString('fr-FR',{month:'long'}).replace(/^./,c=>c.toUpperCase())}
function permissions(){return window.STIPBootCache?.permissions||window.STIPSession?.permissions||{}}
function level(){const p=permissions(),x=Number(p.home_intelligence_level||0);if(x>=1&&x<=3)return x;if(p.admin)return 3;if(p.responsable)return 2;return 1}
function personalRows(){return (window.STIPBootCache?.personal||[]).slice().sort((a,b)=>String(a.date).localeCompare(String(b.date)))}
function codeOf(r){return String(r?.code||r?.source_value||'').trim().toUpperCase()||'—'}
function noticeCount(){const n=Number($('#cpBell span,.hc-bell span')?.textContent||0);return Number.isFinite(n)?n:0}
function pushRange(out,start,end,make){let d=String(start||'').slice(0,10),last=String(end||start||'').slice(0,10),guard=0;if(!d)return;while(d<=last&&guard++<93){out.push(make(d));const x=dObj(d);x.setDate(x.getDate()+1);d=x.toISOString().slice(0,10)}}
function isDone(x){const s=String(x?.status||x?.statut||'').toLowerCase();return !!(x?.completed_at||x?.cancelled_at||x?.resolved_at||['done','termine','terminé','cancelled','annule','annulé','resolved','traite','traité'].includes(s))}
function allItems(){const b=window.STIPBootCache||{},out=[],today=iso();(b.agenda_items||[]).forEach(x=>{if(isDone(x))return;const date=String(x.event_date||'').slice(0,10);if(date>=today)out.push({date,kind:'info',rank:x.importance==='urgent'?0:x.importance==='important'?1:3,icon:x.importance==='urgent'?'⚠️':x.importance==='important'?'❗':'📌',title:x.title||'Information',summary:x.all_day?'Toute la journée':[String(x.start_time||'').slice(0,5),String(x.end_time||'').slice(0,5)].filter(Boolean).join('–'),raw:x})});(b.personal_formations||[]).forEach(x=>{if(isDone(x))return;pushRange(out,x.date_debut,x.date_fin||x.date_debut,date=>({date,kind:'formation',rank:2,icon:'🎓',title:x.intitule||'Formation',summary:[x.horaire,x.lieu].filter(Boolean).join(' · '),raw:x}))});(b.personal_stagiaires||[]).forEach(x=>{if(isDone(x))return;pushRange(out,x.date_debut,x.date_fin||x.date_debut,date=>({date,kind:'stagiaire',rank:2,icon:'👶',title:`Stagiaire · ${[x.prenom,x.nom].filter(Boolean).join(' ')}`,summary:x.horaires||'',raw:x})});return out.filter(x=>x.date>=today).sort((a,b)=>a.date.localeCompare(b.date)||a.rank-b.rank||String(a.title).localeCompare(String(b.title),'fr'))}
function monthStats(){const key=iso().slice(0,7),rows=personalRows().filter(x=>String(x.date||'').startsWith(key)),counts={},worked=[];for(const r of rows){const c=codeOf(r);counts[c]=(counts[c]||0)+1;if(c!=='—'&&!REST.has(c))worked.push(c)}const rests=rows.filter(r=>REST.has(codeOf(r))&&codeOf(r)!=='—').length,hours=worked.reduce((n,c)=>n+(HOURS[c]||0),0),top=Object.entries(counts).filter(([c])=>c!=='—').sort((a,b)=>b[1]-a[1]).slice(0,3);return{rows,worked:worked.length,rests,hours,counts,top}}
function card(kind,key,eyebrow,title,text,meta=''){return`<button type="button" class="hc-intel-card ${kind}" data-intel="${key}"><span>${esc(eyebrow)}</span><strong>${esc(title)}</strong><p>${esc(text)}</p>${meta?`<small>${esc(meta)}</small>`:''}<b class="hc-chevron">›</b></button>`}
function personalCard(){const today=iso(),items=allItems().filter(x=>x.date===today),n=noticeCount();if(n||items.length){const first=items[0],total=n+items.length;const title=total>1?`${total} éléments à voir aujourd’hui`:(first?.title||`${n} élément à traiter`);const text=first?first.title:(n?'Une action attend ton attention.':'');const meta=first?.summary||'';return card('attention','personal','PRIORITAIRE',title,text,meta)}const s=monthStats(),top=s.top.map(([c,n])=>`${c} × ${n}`).join(' · ');return card('personal','personal',monthName().toUpperCase(),`${s.worked} jours travaillés · ${s.rests} repos`,top||'Planning du mois',s.hours?`≈ ${Math.round(s.hours)} h prévues`:'Toucher pour le récapitulatif complet')}
function teamSnapshot(){const b=window.STIPBootCache||{},today=iso(),sources=[b.team_today,b.equipe_today,b.team,b.team_planning].filter(Array.isArray);for(const rows of sources){const day=rows.filter(x=>!x.date||String(x.date).slice(0,10)===today);if(day.length){const work=day.filter(x=>!REST.has(codeOf(x))).length;return{total:day.length,work}}}return null}
function managerCard(){const snap=teamSnapshot(),n=noticeCount(),next=allItems()[0];if(n)return card('attention','manager','À TRAITER',`${n} demande${n>1?'s':''} en attente`,'Une action responsable est nécessaire.',next?`Puis : ${next.title}`:'');if(snap)return card('manager','manager','ÉQUIPE AUJOURD’HUI',`${snap.work}/${snap.total} en activité`,'Vue rapide de la couverture actuelle.','Toucher pour le détail');if(next)return card('manager','manager','À ANTICIPER',next.title,fmtDate(next.date,true),next.summary);return card('manager','manager','ÉQUIPE','Aucun point prioritaire','Rien ne demande une action immédiate.','Toucher pour ouvrir la vue responsable')}
function health(){const b=window.STIPBootCache||{},a=b.agent||window.STIPSession?.agent||{},problems=[];if(!a.telephone)problems.push('Téléphone manquant');if(!(a.email||a.email_pro))problems.push('E-mail manquant');const av=b.media?.avatars?.[a.source_key]||a.avatar_url;if(!av)problems.push('Avatar manquant');return{problems,events:allItems().length,permissions:Object.values(permissions()).filter(Boolean).length}}
function adminCard(){const h=health();if(h.problems.length)return card('attention','admin','QUALITÉ DES DONNÉES',`${h.problems.length} point${h.problems.length>1?'s':''} à corriger`,h.problems[0],h.problems.length>1?`+ ${h.problems.length-1} autre${h.problems.length>2?'s':''}`:'');return card('admin','admin','DONNÉES',`${h.events} événement${h.events>1?'s':''} actif${h.events>1?'s':''}`,`${h.permissions} accès actifs pour ce profil`,'Toucher pour contrôler')}
function rowsHtml(rows){return rows.filter(x=>x[1]!=null&&String(x[1]).trim()!=='').map(([k,v])=>`<div class="hc-detail-row"><span>${esc(k)}</span><strong>${esc(v)}</strong></div>`).join('')}
function eventDetail(item){const x=item.raw||{},common=[['Date',fmtDate(item.date)],['Type',item.kind==='formation'?'Formation':item.kind==='stagiaire'?'Stagiaire':'Information'],['Intitulé',item.title]];let extra=[];if(item.kind==='formation')extra=[['Horaire',x.horaire],['Lieu',x.lieu],['Début',x.date_debut?fmtDate(x.date_debut):''],['Fin',x.date_fin?fmtDate(x.date_fin):''],['Organisme',x.organisme],['Commentaire',x.commentaire||x.notes]];else if(item.kind==='stagiaire')extra=[['Nom',[x.prenom,x.nom].filter(Boolean).join(' ')],['Horaires',x.horaires],['Début',x.date_debut?fmtDate(x.date_debut):''],['Fin',x.date_fin?fmtDate(x.date_fin):''],['École',x.ecole||x.etablissement],['Service',x.service],['Commentaire',x.commentaire||x.notes]];else extra=[['Horaire',x.all_day?'Toute la journée':[x.start_time,x.end_time].filter(Boolean).join(' – ')],['Détail',x.body||x.description||x.details],['Importance',x.importance],['Lieu',x.location||x.lieu]];return `<header><span>${item.icon}</span><div><small>${esc(fmtDate(item.date))}</small><h3>${esc(item.title)}</h3></div></header>${rowsHtml([...common,...extra])}`}
function eventLinks(items){return items.map((x,i)=>`<button class="hc-detail-link" data-detail-index="${i}"><span>${x.icon}</span><b>${esc(x.title)}</b><small>${esc(fmtDate(x.date,true))}</small></button>`).join('')}
function monthSummary(){const s=monthStats(),parts=Object.entries(s.counts).filter(([c])=>c!=='—').sort((a,b)=>b[1]-a[1]);return `<section class="hc-month-summary"><div><strong>${s.worked}</strong><span>jours travaillés</span></div><div><strong>${s.rests}</strong><span>repos</span></div>${s.hours?`<div><strong>${Math.round(s.hours)} h</strong><span>prévues</span></div>`:''}</section>${parts.length?`<div class="hc-shift-breakdown">${parts.map(([c,n])=>`<span><b>${esc(c)}</b>${n}</span>`).join('')}</div>`:''}`}
function personalDetail(){const items=allItems(),today=iso(),endWeek=dObj(today);endWeek.setDate(endWeek.getDate()+7);const weekEnd=endWeek.toISOString().slice(0,10),todayItems=items.filter(x=>x.date===today),weekItems=items.filter(x=>x.date>today&&x.date<=weekEnd),monthKey=today.slice(0,7),monthItems=items.filter(x=>x.date.startsWith(monthKey)&&x.date>weekEnd);const priority=todayItems.filter(x=>x.rank<=1);const blocks=[];if(priority.length)blocks.push(`<h4>Prioritaire</h4>${eventLinks(priority)}`);if(todayItems.length)blocks.push(`<h4>Aujourd’hui</h4>${eventLinks(todayItems)}`);if(weekItems.length)blocks.push(`<h4>Cette semaine</h4>${eventLinks(weekItems)}`);if(monthItems.length)blocks.push(`<h4>Ce mois-ci</h4>${eventLinks(monthItems)}`);blocks.push(`<h4>Récapitulatif</h4>${monthSummary()}`);return `<h3>Mon mois</h3>${blocks.join('')}`}
function managerDetail(){const snap=teamSnapshot(),n=noticeCount(),items=allItems().slice(0,6);return `<h3>Vue responsable</h3>${rowsHtml([['Demandes à traiter',n],['Agents visibles aujourd’hui',snap?.total],['En activité',snap?.work]])}${items.length?`<h4>À anticiper</h4>${eventLinks(items)}`:'<p>Aucun événement proche détecté.</p>'}`}
function adminDetail(){const h=health();return `<h3>Contrôle des données</h3>${rowsHtml([['Événements actifs',h.events],['Accès actifs',h.permissions],['État',h.problems.length?`${h.problems.length} point(s) à corriger`:'Aucune anomalie locale détectée']])}${h.problems.length?`<h4>À corriger</h4><div class="hc-detail-alerts">${h.problems.map(x=>`<p>⚠️ ${esc(x)}</p>`).join('')}</div>`:''}`}
function openPanel(title,html){const p=$('#hsPanel'),body=$('#hsPanelBody'),t=$('#hsPanelTitle');if(!p||!body||!t)return;t.textContent=title;body.innerHTML=`<div class="hc-detail">${html}</div>`;p.classList.add('open');p.setAttribute('aria-hidden','false');bindDetailLinks()}
function bindDetailLinks(){const items=allItems();document.querySelectorAll('.hc-detail-link').forEach(b=>b.onclick=()=>{const item=items[Number(b.dataset.detailIndex)];if(item)openPanel('Détail',eventDetail(item))})}
function openIntel(key){if(key==='personal')openPanel('Mon mois',personalDetail());else if(key==='manager')openPanel('Vue responsable',managerDetail());else openPanel('Contrôle STIP',adminDetail())}
function bindRetain(){const items=allItems().slice(0,6),nodes=[...document.querySelectorAll('#homeView .hc-retain article')];nodes.forEach((el,i)=>{el.setAttribute('role','button');el.setAttribute('tabindex','0');const open=()=>items[i]&&openPanel('Détail',eventDetail(items[i]));el.onclick=open;el.onkeydown=e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();open()}}})}
function render(){const retain=$('#homeView .hc-retain'),cols=$('#homeView .hc-columns');if(!retain||!cols)return;let right=cols.querySelector('.hc-right');if(!right){right=document.createElement('div');right.className='hc-right';retain.replaceWith(right);right.appendChild(retain)}let stack=right.querySelector('.hc-intelligence');if(!stack){stack=document.createElement('section');stack.className='hc-intelligence';right.appendChild(stack)}else if(retain.nextElementSibling!==stack)right.appendChild(stack);const cards=[personalCard()];if(level()>=2)cards.push(managerCard());if(level()>=3)cards.push(adminCard());stack.innerHTML=cards.join('');stack.querySelectorAll('[data-intel]').forEach(b=>b.onclick=()=>openIntel(b.dataset.intel));bindRetain()}
function schedule(){if(raf)return;raf=requestAnimationFrame(()=>{raf=0;render()})}
window.addEventListener('stip:boot-updated',()=>setTimeout(schedule,0));window.addEventListener('stip:session-ready',()=>setTimeout(schedule,0));window.addEventListener('stip:route',e=>{if(e.detail?.route==='home')setTimeout(schedule,0)});document.addEventListener('visibilitychange',()=>{if(!document.hidden)setTimeout(schedule,0)});
const s=document.createElement('style');s.textContent=`.hc-right{min-width:0;display:grid;gap:10px}.hc-intelligence{display:grid;gap:8px}.hc-intel-card{position:relative;width:100%;border:1px solid #dbe8eb;border-radius:17px;background:#fff;box-shadow:0 7px 18px rgba(21,67,82,.06);padding:12px 28px 12px 12px;color:#153a4a;text-align:left;font:inherit}.hc-intel-card>span{display:block;font-size:.59rem;font-weight:950;letter-spacing:.1em;color:#0a91aa}.hc-intel-card>strong{display:block;margin-top:4px;font-size:1rem;line-height:1.15}.hc-intel-card>p{margin:5px 0 0;font-size:.76rem;line-height:1.3;color:#526d77}.hc-intel-card>small{display:block;margin-top:4px;font-size:.66rem;line-height:1.25;color:#7a8e96}.hc-chevron{position:absolute;right:11px;top:50%;transform:translateY(-50%);font-size:1.3rem;color:#8ba0a8}.hc-intel-card.attention{border-color:#f1c6ca;background:#fff9f9}.hc-intel-card.attention>span{color:#d84f5c}.hc-intel-card.manager{background:linear-gradient(145deg,#fbfeff,#f5fbfc)}.hc-intel-card.admin{background:linear-gradient(145deg,#f8f7ff,#f4f6fb)}#homeView .hc-retain article{position:relative;cursor:pointer;padding-right:24px}#homeView .hc-retain article:after{content:'›';position:absolute;right:8px;top:50%;transform:translateY(-50%);font-size:1.15rem;color:#8ba0a8}#homeView .hc-retain article strong{font-size:.84rem!important;line-height:1.18!important}#homeView .hc-retain article small{display:none!important}#homeView .hc-retain article{min-height:58px}.hc-detail{padding:4px 2px 26px;color:#153a4a}.hc-detail>header{display:flex;gap:12px;align-items:flex-start;padding:8px 0 14px}.hc-detail>header>span{width:46px;height:46px;border-radius:13px;background:#eef5f7;display:grid;place-items:center;font-size:1.45rem}.hc-detail h3{margin:0;font-size:1.2rem}.hc-detail header small{display:block;margin-bottom:4px;color:#73878f}.hc-detail h4{margin:18px 0 8px;font-size:.8rem;letter-spacing:.04em;text-transform:uppercase;color:#5e7882}.hc-detail-row{display:grid;grid-template-columns:minmax(90px,.8fr) minmax(0,1.2fr);gap:10px;padding:11px 0;border-bottom:1px solid #e5edef}.hc-detail-row span{font-size:.72rem;color:#71868e}.hc-detail-row strong{font-size:.78rem;line-height:1.3;overflow-wrap:anywhere}.hc-detail-link{width:100%;display:grid;grid-template-columns:34px minmax(0,1fr) auto;gap:8px;align-items:center;border:1px solid #e0eaec;background:#fff;border-radius:12px;padding:9px;margin:7px 0;color:#153a4a;text-align:left}.hc-detail-link span{font-size:1.05rem}.hc-detail-link b{font-size:.76rem}.hc-detail-link small{font-size:.64rem;color:#748991}.hc-detail-alerts p{margin:7px 0;padding:9px;border-radius:10px;background:#fff5f5;font-size:.74rem}.hc-month-summary{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:7px}.hc-month-summary>div{padding:10px 7px;border-radius:12px;background:#eef7f8;text-align:center}.hc-month-summary strong,.hc-month-summary span{display:block}.hc-month-summary strong{font-size:1rem}.hc-month-summary span{margin-top:3px;font-size:.6rem;color:#6f858d}.hc-shift-breakdown{display:flex;flex-wrap:wrap;gap:6px;margin-top:9px}.hc-shift-breakdown span{display:flex;gap:5px;align-items:center;padding:6px 8px;border-radius:999px;background:#f4f8f9;font-size:.65rem}.hc-shift-breakdown b{font-size:.68rem}@media(max-width:370px){.hc-intel-card{padding:10px 26px 10px 10px}.hc-intel-card>strong{font-size:.93rem}.hc-intel-card>p{font-size:.71rem}#homeView .hc-retain article strong{font-size:.78rem!important}}`;document.head.appendChild(s);schedule()})();