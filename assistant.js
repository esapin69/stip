(()=>{'use strict';const API='https://yzsrmuxghlengnkyphxj.supabase.co/functions/v1/stip-assistant',STORE='stip_session_v1';let items=[];const $=s=>document.querySelector(s),esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));function iso(d){return new Intl.DateTimeFormat('en-CA',{year:'numeric',month:'2-digit',day:'2-digit',timeZone:'Europe/Paris'}).format(d)}function fmtDate(s){try{return new Intl.DateTimeFormat('fr-FR',{weekday:'short',day:'numeric',month:'short'}).format(new Date(String(s).slice(0,10)+'T12:00:00')).replace(/\./g,'')}catch{return s||''}}function label(x){if(x.severity>=3)return'À DÉCIDER';if(x.kind==='opportunity'||x.kind==='proposal')return'OPPORTUNITÉ';if(x.kind==='anticipation'||x.kind==='warning')return'À ANTICIPER';return'INFORMATION'}function group(x){if(x.severity>=3)return'decision';if(x.kind==='opportunity'||x.kind==='proposal')return'opportunity';if(x.kind==='anticipation'||x.kind==='warning')return'anticipation';return'info'}function confidence(x){if(x.source_family==='strategy'||x.kind==='proposal')return'Piste à vérifier';if(x.context)return'Conclusion étayée';return'Fait détecté'}function normalizeShift(v){const s=String(v||'').trim().toUpperCase().replace(/\s+/g,'');if(['M','MATIN'].includes(s))return'Matin · M';if(['J','JOUR','JOURNEE','JOURNÉE'].includes(s))return'Journée · J';if(['J4'].includes(s))return'J4';if(['S','SOIR'].includes(s))return'Soir · S';if(['N','NUIT'].includes(s))return'Nuit · N';return''}function scope(x){const c=x?.context||{};for(const k of ['shift','shift_code','code','horaire','period','periode','période','plage']){const s=normalizeShift(c[k]);if(s)return s}const hay=[x?.title,x?.body,c?.label,c?.name].filter(Boolean).join(' ');const m=hay.match(/(?:^|\s|[·:()\-])(J4|M|J|S|N)(?=$|\s|[·:()\-])/i);if(m)return normalizeShift(m[1]);if(x?.source_family==='staffing'||x?.source_family==='compound')return'Journée · total';return''}function why(x){const c=x.context;if(!c)return x.body||'Cette observation provient des données disponibles dans STIP.';const parts=[];for(const [k,v] of Object.entries(c)){if(v==null||typeof v==='object'||k.includes('id'))continue;const n=k.replaceAll('_',' ');parts.push(`${n} : ${v}`);if(parts.length===5)break}return parts.length?parts.join(' · '):(x.body||'Données STIP croisées.')}function render(filter='all'){const list=filter==='all'?items:items.filter(x=>group(x)===filter);$('#feed').innerHTML=list.length?list.map(x=>{const sc=scope(x);return`<article class="as-card" data-group="${group(x)}"><header><div><span class="as-tag">${label(x)}</span><h3>${esc(x.title)}</h3></div><time>${esc(fmtDate(x.date))}</time></header>${sc?`<div class="as-scope"><span>Créneau</span><strong>${esc(sc)}</strong></div>`:''}<p class="body">${esc(x.body||'')}</p>${x.recommendation_text?`<div class="as-rec"><strong>Proposition</strong><span>${esc(x.recommendation_text)}</span></div>`:''}<span class="as-confidence">${confidence(x)}</span><details class="as-why"><summary>Pourquoi cette alerte ?</summary><p>${esc(why(x))}</p></details></article>`}).join(''):'<p class="as-empty">Rien d’utile à signaler dans cette catégorie.</p>'}async function load(){const t=localStorage.getItem(STORE)||'';if(!t){location.href='index.html';return}const start=new Date(),end=new Date();end.setDate(end.getDate()+30);try{const r=await fetch(API,{method:'POST',headers:{'Content-Type':'application/json','X-STIP-Session':t},body:JSON.stringify({action:'feed',start_date:iso(start),end_date:iso(end)}),cache:'no-store'});const j=await r.json();if(!r.ok)throw Error(j.error||'Lecture impossible');if(!j.enabled){$('#headline').textContent='Assistant désactivé pour cet accès';$('#meta').textContent='Aucune analyse n’est exposée.';items=[];render();return}items=j.items||[];$('#headline').textContent=j.summary?.headline||'Aucun point prioritaire détecté';const s=j.summary||{},b=[];if(s.critical_count)b.push(`${s.critical_count} à décider`);if(s.attention_count)b.push(`${s.attention_count} à anticiper`);if(s.opportunity_count)b.push(`${s.opportunity_count} opportunité${s.opportunity_count>1?'s':''}`);$('#meta').textContent=b.join(' · ')||'Situation normale : l’Assistant reste silencieux.';render()}catch(e){$('#error').textContent=e.message||String(e);$('#feed').innerHTML='<p class="as-empty">Impossible de charger le guidage.</p>'}}$('#backBtn').onclick=()=>history.length>1?history.back():location.assign('index.html');document.querySelectorAll('.as-filters button').forEach(b=>b.onclick=()=>{document.querySelectorAll('.as-filters button').forEach(x=>x.classList.remove('active'));b.classList.add('active');render(b.dataset.filter)});load()})();