(()=>{'use strict';
function ensure(){const head=document.querySelector('#homeView .cp-head'),bell=document.querySelector('#homeView #cpBell');if(!head||!bell)return;document.querySelector('#homeView .cp-profile [data-profile-logout]')?.remove();let actions=head.querySelector('.cp-head-actions');if(!actions){actions=document.createElement('div');actions.className='cp-head-actions';bell.before(actions);actions.appendChild(bell)}if(actions.querySelector('[data-profile-logout]'))return;const btn=document.createElement('button');btn.type='button';btn.dataset.profileLogout='1';btn.className='cp-head-logout';btn.innerHTML='<span>Déconnexion</span><b>→</b>';btn.setAttribute('aria-label','Se déconnecter de STIP');btn.addEventListener('click',()=>document.getElementById('logoutBtn')?.click());actions.appendChild(btn)}
let queued=false;function schedule(){if(queued)return;queued=true;requestAnimationFrame(()=>{queued=false;ensure()})}new MutationObserver(schedule).observe(document.querySelector('#homeView')||document.body,{childList:true,subtree:true});window.addEventListener('stip:session-ready',schedule);window.addEventListener('stip:boot-updated',schedule);setTimeout(ensure,100);
const st=document.createElement('style');st.textContent=`.cp-head-actions{display:flex;align-items:center;gap:7px;flex:0 0 auto}.cp-head-logout{height:52px;border:1px solid rgba(220,235,237,.8);border-radius:16px;background:#fff;color:#315866;display:flex;align-items:center;gap:6px;box-shadow:var(--cp-shadow);padding:0 11px;font:inherit;font-size:.68rem;font-weight:900;white-space:nowrap}.cp-head-logout b{font-size:1rem}.cp-head-logout:active{transform:translateY(1px)}@media(max-width:390px){.cp-head-actions{gap:5px}.cp-head-logout{height:48px;padding:0 8px;font-size:.62rem}.cp-bell{width:48px!important;height:48px!important}}`;document.head.appendChild(st)})();