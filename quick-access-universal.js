(()=>{'use strict';
if(/(?:^|\/)index\.html$/.test(location.pathname)||location.pathname==='/'||location.pathname==='')return;
const API='https://yzsrmuxghlengnkyphxj.supabase.co/functions/v1/stip-access',STORE='stip_session_v1',USAGE='stip_app_usage_v1',FAV='stip_app_favorites_v1';
const I={home:'<svg viewBox="0 0 24 24" aria-hidden="true"><path d="M3.5 10.5 12 3l8.5 7.5"/><path d="M5.5 9.5V21h13V9.5M9.5 21v-6h5v6"/></svg>',profile:'<svg viewBox="0 0 24 24" aria-hidden="true"><rect x="3" y="4" width="18" height="16" rx="3"/><circle cx="9" cy="10" r="2.5"/><path d="M5.8 17c.6-2.3 1.8-3.5 3.2-3.5s2.7 1.2 3.2 3.5M15 9h3M15 13h3"/></svg>',fav:'<svg viewBox="0 0 24 24" aria-hidden="true"><path d="m12 3 2.7 5.5 6.1.9-4.4 4.3 1 6.1-5.4-2.9-5.4 2.9 1-6.1-4.4-4.3 6.1-.9z"/></svg>'};
const APPS={places:{label:'Visiter les lieux',home:'places-app.html'},responsable:{label:'Responsable',home:'responsable.html'},assistant:{label:'Assistant STIP',home:'assistant.html'},activity:{label:'Activité',home:'cadre-activite.html'},access:{label:'Accès',home:'access-manage.html'},personal:{label:'Planning perso',home:'index.html?quick=personal'},team:{label:'Planning équipe',home:'index.html?quick=team'},compare:{label:'Comparer les plannings',home:'planning-compare-app.html'},change:{label:'Changement',home:'index.html?quick=change'},calendar:{label:'Mon agenda',home:'index.html?quick=calendar'},contacts:{label:'Contacts',home:'index.html?quick=contacts'},newagent:{label:'Nouvel agent',home:'https://esapin69.github.io/Ghe-interne/'},upload:{label:'Importer',home:'https://admin-ghe.esapin.com/depot.html'},admin:{label:'Admin',home:'https://admin-ghe.esapin.com/'}};
const PERM={places:'places',responsable:'responsable',assistant:'assistant_enabled',activity:'activity',access:'access_manage',personal:'planning_personal',team:'planning_team',compare:'planning_team',change:'change_app',calendar:'calendar_subscribe',contacts:'contacts',newagent:'nouveaux_arrivants',upload:'file_upload',admin:'admin'};
function currentKey(){const p=location.pathname.split('/').pop()||'';if(p==='places.html'||p==='places-app.html')return'places';if(p==='planning-compare-app.html')return'compare';if(/^responsable(?:-|\.)/.test(p))return'responsable';if(p==='assistant.html')return'assistant';if(p==='cadre-activite.html')return'activity';if(p==='access-manage.html')return'access';return''}
function readUsage(){try{return JSON.parse(localStorage.getItem(USAGE)||'{}')||{}}catch{return{}}}function writeUsage(v){try{localStorage.setItem(USAGE,JSON.stringify(v))}catch{}}function readFav(){try{const x=JSON.parse(localStorage.getItem(FAV)||'[]');return Array.isArray(x)?x.filter(k=>APPS[k]):[]}catch{return[]}}function writeFav(v){try{localStorage.setItem(FAV,JSON.stringify([...new Set(v)].filter(k=>APPS[k])))}catch{}}
function allowed(k,perms){return !!(APPS[k]&&perms?.[PERM[k]])}function touch(k){if(!APPS[k])return;const now=Date.now(),u=readUsage(),apps=u.apps||{},prev=Number(u.at||now),days=Math.max(0,(now-prev)/86400000),decay=Math.pow(.985,days);Object.keys(apps).forEach(x=>apps[x]=Math.max(0,Number(apps[x]||0)*decay));apps[k]=Number(apps[k]||0)+1;writeUsage({at:now,last:k,apps})}function go(url){location.href=new URL(url,location.href).href}function appHome(k,perms){if(k==='places'){const raw=String(perms?.__levels?.places||'visitor').toLowerCase(),mode=['pro','admin','internal','internal_stip','restricted'].includes(raw)?'pro':'visitor';return`places-app.html?mode=${mode}`}return APPS[k]?.home||'index.html'}
function suggestions(perms){const pinned=new Set(readFav().filter(k=>allowed(k,perms))),scores=readUsage().apps||{};return Object.keys(APPS).filter(k=>allowed(k,perms)&&!pinned.has(k)).sort((a,b)=>Number(scores[b]||0)-Number(scores[a]||0)||APPS[a].label.localeCompare(APPS[b].label,'fr')).slice(0,3)}
async function me(){const token=localStorage.getItem(STORE)||'';if(!token)return null;try{const r=await fetch(API,{method:'POST',cache:'no-store',headers:{'Content-Type':'application/json','X-STIP-Session':token},body:JSON.stringify({action:'me'})});if(!r.ok)return null;return await r.json().catch(()=>null)}catch{return null}}
function ensureCss(){if([...document.styleSheets].some(s=>String(s.href||'').includes('quick-access.css')))return;const css=document.createElement('link');css.rel='stylesheet';css.href='quick-access.css?v=20260901-favorites1';document.head.appendChild(css)}
function closeFav(){document.getElementById('stipFavoritesPanel')?.remove();document.body.classList.remove('stip-favorites-open');document.querySelector('[data-u="favorites"]')?.classList.remove('is-current')}
function favRow(k,pinned){return `<div class="stip-fav-row"><button type="button" class="stip-fav-open" data-fav-open="${k}"><span>${APPS[k].label}</span><b>›</b></button><button type="button" class="stip-fav-pin ${pinned?'is-pinned':''}" data-fav-pin="${k}" aria-label="${pinned?'Retirer des favoris':'Épingler'}">${pinned?'★':'☆'}</button></div>`}
function toggleFav(perms){if(document.getElementById('stipFavoritesPanel'))return closeFav();const pinned=readFav().filter(k=>allowed(k,perms)),suggest=suggestions(perms),all=Object.keys(APPS).filter(k=>allowed(k,perms)),p=document.createElement('section');p.id='stipFavoritesPanel';p.className='stip-favorites-panel';p.innerHTML=`<button type="button" class="stip-fav-backdrop" aria-label="Fermer"></button><div class="stip-fav-sheet"><header><div><small>ACCÈS RAPIDE</small><h2>Applications favorites</h2></div><button type="button" class="stip-fav-close" aria-label="Fermer">×</button></header>${pinned.length?`<section><h3>Épinglées</h3><div>${pinned.map(k=>favRow(k,true)).join('')}</div></section>`:''}<section><h3>${pinned.length?'Suggestions':'Suggestions selon votre usage'}</h3><div>${suggest.length?suggest.map(k=>favRow(k,false)).join(''):'<p class="stip-fav-empty">Utilisez vos applications : vos raccourcis apparaîtront ici.</p>'}</div></section><details><summary>Choisir une autre application</summary><div class="stip-fav-all">${all.map(k=>favRow(k,pinned.includes(k))).join('')}</div></details></div>`;document.body.appendChild(p);document.body.classList.add('stip-favorites-open');document.querySelector('[data-u="favorites"]')?.classList.add('is-current');p.querySelector('.stip-fav-backdrop').onclick=closeFav;p.querySelector('.stip-fav-close').onclick=closeFav;p.addEventListener('click',e=>{const o=e.target.closest('[data-fav-open]'),pin=e.target.closest('[data-fav-pin]');if(o){const k=o.dataset.favOpen;closeFav();touch(k);go(appHome(k,perms));return}if(pin){const k=pin.dataset.favPin,a=readFav();writeFav(a.includes(k)?a.filter(x=>x!==k):[...a,k]);closeFav();toggleFav(perms)}})}
function mount(perms){if(document.getElementById('stipQuickUniversal'))return;ensureCss();const k=currentKey();if(k&&allowed(k,perms))touch(k);const dashboard=allowed('responsable',perms);const n=document.createElement('nav');n.id='stipQuickUniversal';n.className='stip-quick-switch';n.setAttribute('aria-label','Navigation STIP');n.innerHTML=`<button type="button" data-u="public" aria-label="${dashboard?'Tableau de bord Responsable':'Accueil STIP'}"><span class="qs-icon">${I.home}</span></button><button type="button" data-u="profile" aria-label="Accueil STIP"><span class="qs-icon">${I.profile}</span></button><button type="button" data-u="favorites" aria-label="Applications favorites"><span class="qs-icon">${I.fav}</span></button>`;n.addEventListener('click',e=>{const b=e.target.closest('[data-u]');if(!b)return;const a=b.dataset.u;if(a==='public')return go(dashboard?'responsable.html?view=dashboard':'index.html?quick=public');if(a==='profile')return go('index.html?quick=profile');if(a==='favorites')return toggleFav(perms)});document.body.appendChild(n);document.body.classList.add('stip-quick-connected','stip-quick-in-app')}
me().then(j=>{const perms=j?.permissions||j?.profile?.permissions||{};if(j)mount(perms)});document.addEventListener('keydown',e=>{if(e.key==='Escape')closeFav()});
})();