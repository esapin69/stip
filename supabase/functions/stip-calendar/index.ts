import 'jsr:@supabase/functions-js/edge-runtime.d.ts'
import { createClient } from 'npm:@supabase/supabase-js@2'

const U=Deno.env.get('SUPABASE_URL')!, K=Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!
const db=createClient(U,K)
const C={'Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'content-type,x-stip-session','Access-Control-Allow-Methods':'GET,HEAD,POST,OPTIONS'}
const J={...C,'Content-Type':'application/json; charset=utf-8','Cache-Control':'no-store'}
const I={...C,'Content-Type':'text/calendar; charset=utf-8','Cache-Control':'no-cache, no-store, must-revalidate','X-Content-Type-Options':'nosniff'}
const json=(b:any,s=200)=>new Response(JSON.stringify(b),{status:s,headers:J})
const enc=new TextEncoder()
const hex=(a:ArrayBuffer)=>[...new Uint8Array(a)].map(b=>b.toString(16).padStart(2,'0')).join('')
async function sha(s:string){return hex(await crypto.subtle.digest('SHA-256',enc.encode(s)))}
const ymd=(o=0)=>{const d=new Date();d.setUTCHours(12,0,0,0);d.setUTCDate(d.getUTCDate()+o);return d.toISOString().slice(0,10)}
const d8=(d:string)=>String(d).replaceAll('-','')
const dayShift=(d:string,n:number)=>{const x=new Date(String(d)+'T12:00:00Z');x.setUTCDate(x.getUTCDate()+n);return x.toISOString().slice(0,10)}
const next=(d:string)=>dayShift(d,1)
const stamp=(v:any)=>new Date(v||Date.now()).toISOString().replace(/[-:]/g,'').replace(/\.\d{3}Z$/,'Z')
const CAL_REV=29
const CAL_BUILD='2026-09-18T17:37:00Z'
function eventStamp(v:any){const a=new Date(v||0),b=new Date(CAL_BUILD);return stamp(a>b?a:b)}
const esc=(v:any)=>String(v??'').replace(/\\/g,'\\\\').replace(/\r?\n/g,'\\n').replace(/,/g,'\\,').replace(/;/g,'\\;')
function fold(line:string,limit=73){const out:string[]=[];let cur='',bytes=0;for(const ch of line){const n=enc.encode(ch).length;if(cur&&bytes+n>limit){out.push(cur);cur=ch;bytes=n}else{cur+=ch;bytes+=n}}if(cur)out.push(cur);return out.join('\r\n ')}
const head=(n:string)=>['BEGIN:VCALENDAR','VERSION:2.0','PRODID:-//STIP//GHE//FR','CALSCALE:GREGORIAN','METHOD:PUBLISH',`X-WR-CALNAME:${esc(n)}`,'X-WR-TIMEZONE:Europe/Paris','REFRESH-INTERVAL;VALUE=DURATION:PT1H','X-PUBLISHED-TTL:PT1H']
function ev(l:string[],u:string,d:string,s:string,x:string,t:any){const es=eventStamp(t);l.push('BEGIN:VEVENT',`UID:${esc(u)}`,`DTSTAMP:${es}`,`LAST-MODIFIED:${es}`,`SEQUENCE:${CAL_REV}`,`DTSTART;VALUE=DATE:${d8(d)}`,`DTEND;VALUE=DATE:${d8(next(d))}`,fold(`SUMMARY:${esc(s)}`),fold(`DESCRIPTION:${esc(x)}`),'TRANSP:TRANSPARENT','STATUS:CONFIRMED','END:VEVENT')}
function mins(t:string){const [h,m]=String(t).split(':').map(Number);return h*60+m}
function evTimed(l:string[],u:string,d:string,a:string,b:string,s:string,x:string,t:any,alarm=false){let ed=d,sm=mins(a),em=mins(b);if(em<=sm)ed=next(d);const aa=a.replace(':','')+'00',bb=b.replace(':','')+'00',es=eventStamp(t);l.push('BEGIN:VEVENT',`UID:${esc(u)}`,`DTSTAMP:${es}`,`LAST-MODIFIED:${es}`,`SEQUENCE:${CAL_REV}`,`DTSTART;TZID=Europe/Paris:${d8(d)}T${aa}`,`DTEND;TZID=Europe/Paris:${d8(ed)}T${bb}`,fold(`SUMMARY:${esc(s)}`),fold(`DESCRIPTION:${esc(x)}`),'TRANSP:TRANSPARENT','STATUS:CONFIRMED');if(alarm)l.push('BEGIN:VALARM','TRIGGER:PT0M','ACTION:DISPLAY',fold(`DESCRIPTION:${esc(s)}`),'END:VALARM');l.push('END:VEVENT')}

const SHIFT:any={M:{dot:'🔵',personal:'🟦',label:'Matin',text:'06:50–14:40',start:'06:50',end:'14:40'},J:{dot:'🟢',personal:'🟩',label:'Jour',text:'08:30–16:20',start:'08:30',end:'16:20'},J4:{dot:'🟠',personal:'🟧',label:'J4',text:'10:10–18:00',start:'10:10',end:'18:00'},S:{dot:'🟡',personal:'🟨',label:'Soir',text:'13:30–21:00',start:'13:30',end:'21:00'},N:{dot:'⚫',personal:'⬛',label:'Nuit',text:'21:00–06:50',start:'21:00',end:'06:50'}}
const ORDER=['M','J','J4','S','N']
const REST=new Set(['RH','RTT','RC','AA','MA','CA','RF','RTA','RTTA','SYR','OFF','REPOS','-',''])
const SICK=new Set(['AR','AT'])
function norm(v:any){const c=String(v??'').trim().toUpperCase();if(REST.has(c)||SICK.has(c)||!c)return null;if(c.startsWith('J4'))return'J4';if(c.startsWith('M'))return'M';if(c.startsWith('J'))return'J';if(c.startsWith('S'))return'S';if(c.startsWith('N'))return'N';return c}
function timeRange(v:any){const s=String(v||'').replace(/[hH]/g,':'),m=s.match(/(\d{1,2})\s*:\s*(\d{2})\D+(\d{1,2})\s*:\s*(\d{2})/);if(!m)return null;const a=`${m[1].padStart(2,'0')}:${m[2]}`,b=`${m[3].padStart(2,'0')}:${m[4]}`;return{start:a,end:b,text:`${a}–${b}`}}
function covers(ref:any,target:any){if(!ref||!target)return true;let rs=mins(ref.start),re=mins(ref.end),ts=mins(target.start),te=mins(target.end);if(re<=rs)re+=1440;if(te<=ts)te+=1440;return rs<=ts&&re>=te}
function overlap(a:any,b:any){if(!a||!b)return false;let as=mins(a.start),ae=mins(a.end),bs=mins(b.start),be=mins(b.end);if(ae<=as)ae+=1440;if(be<=bs)be+=1440;return Math.max(as,bs)<Math.min(ae,be)}
function withinShift(time:string,sh:any){let t=mins(time),a=mins(sh.start),b=mins(sh.end);if(b<=a){if(t<a)t+=1440;b+=1440}return t>=a&&t<b}
function addMinutes(date:string,time:string,delta:number){let x=mins(time)+delta,d=date;while(x<0){x+=1440;d=dayShift(d,-1)}while(x>=1440){x-=1440;d=next(d)}return{date:d,time:`${String(Math.floor(x/60)).padStart(2,'0')}:${String(x%60).padStart(2,'0')}`}}
const plusDuration=(date:string,time:string,duration=15)=>addMinutes(date,time,duration)
const keyNorm=(v:any)=>String(v||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLocaleUpperCase('fr-FR').replace(/[^A-Z0-9]+/g,' ').trim()
function cap(v:any){v=String(v||'').trim();return v?v.charAt(0).toLocaleUpperCase('fr-FR')+v.slice(1).toLocaleLowerCase('fr-FR'):''}
function firstName(a:any){if(a?.prenom)return cap(a.prenom);const k=String(a?.source_key||'').split('_').filter(Boolean);if(k.length)return cap(k.at(-1));const w=String(a?.nom||'').trim().split(/\s+/).filter(Boolean);return cap(w.at(-1)||'Agent')}
function frDate(d:string){return cap(new Intl.DateTimeFormat('fr-FR',{weekday:'long',day:'2-digit',month:'long',year:'numeric',timeZone:'Europe/Paris'}).format(new Date(d+'T12:00:00Z')))}
function frUpdate(v:any){if(!v)return'—';const p=new Intl.DateTimeFormat('fr-FR',{day:'2-digit',month:'2-digit',hour:'2-digit',minute:'2-digit',hour12:false,timeZone:'Europe/Paris'}).formatToParts(new Date(v)),g=(t:string)=>p.find(z=>z.type===t)?.value||'';return`${g('day')}/${g('month')} à ${g('hour')}:${g('minute')}`}
function parisDate(v:any){const p=new Intl.DateTimeFormat('en-CA',{year:'numeric',month:'2-digit',day:'2-digit',timeZone:'Europe/Paris'}).formatToParts(new Date(v)),g=(t:string)=>p.find(z=>z.type===t)?.value||'';return`${g('year')}-${g('month')}-${g('day')}`}
function parisTime(v:any){const p=new Intl.DateTimeFormat('fr-FR',{hour:'2-digit',minute:'2-digit',hour12:false,timeZone:'Europe/Paris'}).formatToParts(new Date(v)),g=(t:string)=>p.find(z=>z.type===t)?.value||'';return`${g('hour')}:${g('minute')}`}
function gheNum(v:any){const m=String(v||'').match(/(\d+)/);return m?Number(m[1]):Infinity}
function gheText(v:any){const n=gheNum(v);return Number.isFinite(n)?`GHE ${String(n).padStart(2,'0')}`:'GHE --'}
function e164(v:any){const raw=String(v||'').trim(),d=raw.replace(/\D/g,'');if(!d)return'';if(raw.startsWith('+'))return'+'+d;if(d.startsWith('00'))return'+'+d.slice(2);if(d.length===10&&d.startsWith('0'))return'+33'+d.slice(1);if(d.startsWith('33')&&d.length===11)return'+'+d;return d.length>=10?('+'+d):''}
function phonePretty(v:any){const p=e164(v);if(!p)return'Téléphone non renseigné';if(p.startsWith('+33')&&p.length===12){const r='0'+p.slice(3);return r.replace(/(\d{2})(?=\d)/g,'$1 ').trim()}return p}
function fullPerson(prenom:any,nom:any){const p=String(prenom||'').trim(),n=String(nom||'').trim();return[p?p.toLocaleUpperCase('fr-FR'):'',n?n.toLocaleLowerCase('fr-FR'):''].filter(Boolean).join(' ')||'INCONNU'}
function person(a:any){if(!a)return'INCONNU';if(a.prenom)return fullPerson(a.prenom,a.nom);const k=String(a.source_key||'').split('_').filter(Boolean);if(k.length>=2)return`${k.at(-1).toLocaleUpperCase('fr-FR')} ${k.slice(0,-1).join(' ').toLocaleLowerCase('fr-FR')}`;const words=String(a.nom||'').trim().split(/\s+/).filter(Boolean);if(words.length>=2)return`${words.at(-1).toLocaleUpperCase('fr-FR')} ${words.slice(0,-1).join(' ').toLocaleLowerCase('fr-FR')}`;return String(a.nom||'AGENT').toLocaleUpperCase('fr-FR')}
function splitRefs(v:any){return String(v||'').split(/\s*(?:\+|\/|;|&|\bet\b)\s*/i).map(x=>x.trim()).filter(Boolean)}
function splitFormationPlaces(v:any,total:number){const raw=String(v||'').trim();if(!raw)return Array(total).fill('Non renseigné');const p=raw.split(/\s*·\s*/).map(x=>x.trim()).filter(Boolean);if(p.length===1)return Array(total).fill(p[0].replace(/^J\d+\s*/i,''));const out:string[]=[];for(let i=0;i<total;i++){const q=p[Math.min(i,p.length-1)]||'';out.push(q.replace(new RegExp(`^J${i+1}\\s*`,'i'),'').trim()||'Non renseigné')}return out}
function sessionKey(r:any){return[String(r.intitule||''),String(r.date_debut||''),String(r.date_fin||''),String(r.horaire||''),String(r.lieu||'')].join('|')}
function teamOf(a:any){const t=String(a?.type_planning||a?.equipe||'jour').toLowerCase();return t==='chef'||t==='chefs'?'chefs':t==='nuit'?'nuit':'jour'}
async function planningPaged(select:string,start:string,end:string,opts:any={}){const out:any[]=[];let from=0;const size=1000;for(;;){let q:any=db.from('planning').select(select).gte('date',start).lte('date',end).order('date').order('agent_id').range(from,from+size-1);if(opts.team)q=q.eq('equipe',opts.team);if(opts.ids?.length)q=q.in('agent_id',opts.ids);const {data,error}=await q;if(error)throw error;const part=data||[];out.push(...part);if(part.length<size)break;from+=size}return out}

async function prof(id:string){const {data:p}=await db.from('stip_access_profiles').select('id,agent_id,active,permissions').eq('id',id).maybeSingle();if(!p?.active||!(p.permissions?.calendar_subscribe||p.permissions?.calendriers))return null;const {data:a}=await db.from('agents').select('id,source_key,nom,prenom,equipe,type_planning,role,ghe,telephone').eq('id',p.agent_id).maybeSingle();return a?{...p,agent:a}:null}
async function session(r:Request){const t=r.headers.get('x-stip-session')||'';if(!t)return null;const {data:s}=await db.from('stip_access_sessions').select('profile_id,expires_at,revoked_at').eq('token_hash',await sha(t)).maybeSingle();return !s||s.revoked_at||new Date(s.expires_at)<=new Date()?null:prof(s.profile_id)}
async function ensure(id:string,k:string){const {data:o}=await db.from('stip_calendar_feeds').select('token').eq('profile_id',id).eq('kind',k).eq('active',true).maybeSingle();if(o?.token)return o.token;const {data,error}=await db.from('stip_calendar_feeds').upsert({profile_id:id,kind:k,active:true,updated_at:new Date().toISOString()},{onConflict:'profile_id,kind'}).select('token').single();if(error)throw error;return data.token}
function personalCalendarName(a:any){return`Mon planning ${firstName(a).toLocaleUpperCase('fr-FR')}`}
function sharedCalendarName(a:any){return`Planning ${person(a)}`}
function sensitiveAgenda(r:any){return /medical|mobi_lit|visite|private_appointment/i.test(String(r?.source_type||''))}
async function activeAgentById(id:string){const {data,error}=await db.from('agents').select('id,source_key,nom,prenom,equipe,type_planning,role,ghe,telephone').eq('id',id).eq('actif',true).maybeSingle();if(error)throw error;return data}
async function activeAgentBySource(sourceKey:string){const {data,error}=await db.from('agents').select('id,source_key,nom,prenom,equipe,type_planning,role,ghe,telephone').eq('source_key',sourceKey).eq('actif',true).maybeSingle();if(error)throw error;return data}
function canSubscribeAgent(p:any,a:any){return Boolean(a&&(String(p.agent?.id||'')===String(a.id)||p.permissions?.responsable||p.permissions?.admin))}

async function loadDirectory(){const [{data:aa,error:ae},{data:cc,error:ce}]=await Promise.all([db.from('agents').select('id,source_key,nom,prenom,equipe,type_planning,role,ghe,telephone').eq('actif',true),db.from('contacts_ghe').select('source_key,ghe,prenom,nom,telephone,equipe,role_metier').eq('actif',true).eq('categorie','personne')]);if(ae)throw ae;if(ce)throw ce;const agents=aa||[],contacts=cc||[],byId=new Map(agents.map((x:any)=>[x.id,x])),bySource=new Map(),byGhe=new Map();for(const c of contacts){if(c.source_key&&!bySource.has(c.source_key))bySource.set(c.source_key,c);const g=gheNum(c.ghe);if(Number.isFinite(g)&&!byGhe.has(String(g)))byGhe.set(String(g),c)}return{agents,contacts,byId,bySource,byGhe}}
function contactFor(a:any,dir:any){return dir.bySource.get(String(a?.source_key||''))||dir.byGhe.get(String(gheNum(a?.ghe)))||null}
function contactLines(a:any,c:any){const tel=c?.telephone||a?.telephone,g=c?.ghe||a?.ghe;return[`☎️ ${phonePretty(tel)}`,`🏷️ ${gheText(g)}`]}
function refMatchesAgent(ref:string,a:any){const q=keyNorm(ref);if(!q)return false;const vals=new Set([keyNorm(a?.prenom),keyNorm(a?.nom),keyNorm(a?.source_key),keyNorm(person(a)),keyNorm(`${a?.prenom||''} ${a?.nom||''}`),keyNorm(`${a?.nom||''} ${a?.prenom||''}`)]);const key=String(a?.source_key||'').split('_').filter(Boolean);if(key.length)vals.add(keyNorm(key.at(-1)));return vals.has(q)}
function resolveRef(ref:string,dir:any){const q=keyNorm(ref);let a=dir.agents.find((x:any)=>refMatchesAgent(ref,x));let c=dir.contacts.find((x:any)=>[keyNorm(x.source_key),keyNorm(x.prenom),keyNorm(x.nom),keyNorm(`${x.prenom||''} ${x.nom||''}`),keyNorm(`${x.nom||''} ${x.prenom||''}`)].includes(q));if(!a&&c?.source_key)a=dir.agents.find((x:any)=>x.source_key===c.source_key);if(!c&&a)c=contactFor(a,dir);return a||c?{a,c}:null}
async function loadChiefPlan(dir:any,start:string,end:string){const chiefs=dir.agents.filter((a:any)=>String(a.equipe||a.type_planning||'').toLowerCase()==='chefs'||String(a.role||'').toLowerCase().includes('chef')),ids=chiefs.map((a:any)=>a.id),map=new Map();if(ids.length){const pp=await planningPaged('date,code,agent_id,imported_at',start,end,{ids});for(const p of pp)map.set(`${p.agent_id}|${p.date}`,p)}return{chiefs,map}}
function chiefsAt(date:string,time:string,chiefData:any,dir:any){const out:any[]=[];for(const a of chiefData.chiefs){const p=chiefData.map.get(`${a.id}|${date}`),k=norm(p?.code);if(k&&SHIFT[k]&&withinShift(time,SHIFT[k]))out.push({a,c:contactFor(a,dir),k})}return out}

async function personal(a:any,shared=false){const start=ymd(-60),end=ymd(370);const [{data:plan,error:pe},{data:manual,error:me},{data:frows,error:fe},{data:srows,error:se},dir]=await Promise.all([db.from('planning').select('date,code,observation,imported_at').eq('agent_id',a.id).gte('date',start).lte('date',end).order('date'),db.from('stip_agent_agenda_items').select('*').eq('agent_id',a.id).eq('status','active').gte('event_date',start).lte('event_date',end).order('event_date').order('start_time'),db.from('formations').select('id,source_key,agent_source_key,intitule,date_debut,date_fin,lieu,statut,updated_at,last_seen_at,horaire').gte('date_fin',start+'T00:00:00Z').lte('date_debut',end+'T23:59:59Z').order('date_debut'),db.from('stagiaires').select('id,nom,prenom,date_debut,date_fin,horaires,referent,updated_at,last_seen_at').gte('date_fin',start).lte('date_debut',end).order('date_debut'),loadDirectory()]);if(pe)throw pe;if(me)throw me;if(fe)throw fe;if(se)throw se;const l=head(shared?sharedCalendarName(a):personalCalendarName(a)),notes=new Map(),baseMap=new Map();for(const r of manual||[]){if(r.display_mode==='day_note'){if(!notes.has(r.event_date))notes.set(r.event_date,[]);notes.get(r.event_date).push(r)}}for(const r of plan||[])baseMap.set(r.date,r);for(const r of plan||[]){const raw=String(r.code||'').trim().toUpperCase(),k=norm(raw);let sum='',desc='';if(k&&SHIFT[k]){const q=SHIFT[k],variant=raw&&raw!==k?raw:'';sum=`${q.personal} ${q.label} · ${q.text}`;desc=[`Horaire : ${q.text}`,variant?`Code planning : ${variant}`:'',!shared&&r.observation?`Info : ${r.observation}`:''].filter(Boolean).join('\n')}else if(SICK.has(raw)){sum=shared?'Absence':raw||'Absence';desc=shared?'Absence':`Absence : ${raw||'—'}`}else{sum=raw||'Repos';desc=`Repos : ${raw||'Repos'}`}const ns=notes.get(r.date)||[];for(const n of ns){const sensitive=shared&&sensitiveAgenda(n),title=sensitive?'Rendez-vous':n.title;desc+=`${desc?'\n\n•••\n\n':''}📌 ${title}${!sensitive&&n.body?`\n${n.body}`:''}${!sensitive&&n.location?`\n📍 ${n.location}`:''}`}ev(l,`stip-personal-${a.id}-${r.date}@esapin.com`,r.date,sum,desc,r.imported_at)}for(const [date,ns] of notes){if(baseMap.has(date))continue;const desc=ns.map((n:any)=>{const sensitive=shared&&sensitiveAgenda(n),title=sensitive?'Rendez-vous':n.title;return`📌 ${title}${!sensitive&&n.body?`\n${n.body}`:''}${!sensitive&&n.location?`\n📍 ${n.location}`:''}`}).join('\n\n•••\n\n');ev(l,`stip-personal-note-${a.id}-${date}@esapin.com`,date,'📌 Information STIP',desc,ns.map((x:any)=>x.updated_at).sort().at(-1))}for(const r of manual||[]){if(r.display_mode==='day_note')continue;const sensitive=shared&&sensitiveAgenda(r),icon=sensitive?'📅':r.importance==='urgent'?'⚠️':r.importance==='important'?'❗':'📌',title=sensitive?'Rendez-vous':r.title,desc=sensitive?'':[r.body||'',r.location?`📍 ${r.location}`:''].filter(Boolean).join('\n');if(r.all_day)ev(l,`stip-agenda-${r.id}@esapin.com`,r.event_date,`${icon} ${title}`,desc,r.updated_at);else evTimed(l,`stip-agenda-${r.id}@esapin.com`,r.event_date,String(r.start_time).slice(0,5),String(r.end_time).slice(0,5),`${icon} ${title}`,desc,r.updated_at,false)}const chiefData=await loadChiefPlan(dir,start,end),auto:any[]=[],fg=new Map();for(const r of frows||[]){const k=sessionKey(r);if(!fg.has(k))fg.set(k,[]);fg.get(k).push(r)}for(const rs of fg.values()){if(!rs.some((r:any)=>String(r.agent_source_key||'')===String(a.source_key||'')))continue;const f:any=rs[0],sd=parisDate(f.date_debut),ed=parisDate(f.date_fin),range=timeRange(f.horaire)||{start:parisTime(f.date_debut),end:parisTime(f.date_fin),text:`${parisTime(f.date_debut)}–${parisTime(f.date_fin)}`},days:string[]=[];let d=sd,g=0;while(d<=ed&&g++<40){days.push(d);d=next(d)}const places=splitFormationPlaces(f.lieu,days.length),colleagues=rs.filter((r:any)=>String(r.agent_source_key||'')!==String(a.source_key||'')).map((r:any)=>{const aa=dir.agents.find((x:any)=>x.source_key===r.agent_source_key);return{a:aa||{source_key:r.agent_source_key},c:contactFor(aa,dir)}});days.forEach((date:string,i:number)=>auto.push({type:'formation',date,start:range.start,end:range.end,time:range.text,title:String(f.intitule||'Formation'),place:places[i],colleagues,stamp:rs.map((x:any)=>x.updated_at||x.last_seen_at).filter(Boolean).sort().at(-1)}))}for(const r of srows||[]){const refs=splitRefs(r.referent);if(!refs.some(x=>refMatchesAgent(x,a)))continue;const range=timeRange(r.horaires);if(!range)continue;const colleagues=refs.filter(x=>!refMatchesAgent(x,a)).map(x=>{const z=resolveRef(x,dir);return{a:z?.a||{nom:x},c:z?.c}});let d=String(r.date_debut||''),ed=String(r.date_fin||r.date_debut||''),g=0;while(d&&d<=ed&&g++<370){if(d>=start&&d<=end)auto.push({type:'stagiaire',date:d,start:range.start,end:range.end,time:range.text,title:fullPerson(r.prenom,r.nom),place:'',colleagues,stamp:r.updated_at||r.last_seen_at});d=next(d)}}let autoEventCount=0;for(const x of auto){const p=baseMap.get(x.date),pk=norm(p?.code),shift=pk&&SHIFT[pk]?SHIFT[pk]:null;let strong=addMinutes(x.date,x.start,-60);if(shift&&strong.date===x.date&&mins(strong.time)<mins(shift.start))strong={date:x.date,time:shift.start};const simpleStart=shift?{date:x.date,time:shift.start}:null,strongEnd=plusDuration(strong.date,strong.time,15),chiefs=chiefsAt(x.date,x.start,chiefData,dir),lines=[x.type==='formation'?`🎓 ${x.title}`:`👶 ${x.title}`,`🕒 ${x.time}`];if(x.place)lines.push(`📍 ${x.place}`);if(!shared&&x.colleagues.length){lines.push('','•••','','👥 Collègues concernés','');for(const z of x.colleagues){lines.push(`👨‍⚕️ ${person(z.a||z.c)}`,...contactLines(z.a,z.c),'')}}if(!shared){lines.push('','•••','','👔 Chef d’équipe présent','');if(chiefs.length)for(const z of chiefs)lines.push(`👨‍⚕️ ${person(z.a)}`,...contactLines(z.a,z.c),'');else lines.push('⚠️ Chef d’équipe non retrouvé pour ce créneau')};const delta=strong.date===x.date?mins(x.start)-mins(strong.time):999,strongTitle=`⚠️ ${x.type==='formation'?'FORMATION':'STAGIAIRE'} ${delta===60?'DANS 1 H':delta>0&&delta<999?`DANS ${delta} MIN`:delta===0?'MAINTENANT':'À VENIR'} · ${x.start}`;evTimed(l,`stip-auto-${x.type}-strong-${a.id}-${x.date}-${keyNorm(x.title).replace(/\s+/g,'-')}-${x.start}@esapin.com`,strong.date,strong.time,strongEnd.time,strongTitle,lines.join('\n'),x.stamp,true);autoEventCount++;if(simpleStart&&!(simpleStart.date===strong.date&&simpleStart.time===strong.time)){const se=plusDuration(simpleStart.date,simpleStart.time,10),title=x.type==='formation'?`ℹ️ Formation aujourd’hui · ${x.start}`:`👶 Stagiaire aujourd’hui · ${x.start}`;evTimed(l,`stip-auto-${x.type}-start-${a.id}-${x.date}-${keyNorm(x.title).replace(/\s+/g,'-')}-${x.start}@esapin.com`,simpleStart.date,simpleStart.time,se.time,title,[x.type==='formation'?`🎓 ${x.title}`:`👶 ${x.title}`,`🕒 ${x.time}`,x.place?`📍 ${x.place}`:''].filter(Boolean).join('\n'),x.stamp,true);autoEventCount++}}l.push('END:VCALENDAR');return{ics:l.join('\r\n')+'\r\n',events:(plan||[]).length+(manual||[]).filter((x:any)=>x.display_mode!=='day_note').length+[...notes.keys()].filter(d=>!baseMap.has(d)).length+autoEventCount}}

async function team(a:any){const team=teamOf(a),start=ymd(0),end=ymd(370),dir=await loadDirectory(),rows=await planningPaged('date,code,agent_id,imported_at',start,end,{team});const days=new Map();for(const r of rows){if(!days.has(r.date))days.set(r.date,[]);days.get(r.date).push(r)}const l=head("Esprit d'équipe");let events=0;for(const [date,rs] of days){const groups=new Map(ORDER.map(k=>[k,[]]));for(const r of rs){const k=norm(r.code);if(!k||!SHIFT[k])continue;const ag=dir.byId.get(r.agent_id);if(ag)groups.get(k).push({a:ag,c:contactFor(ag,dir)})}for(const k of ORDER)groups.get(k).sort((x:any,y:any)=>gheNum(x.c?.ghe||x.a.ghe)-gheNum(y.c?.ghe||y.a.ghe)||person(x.a).localeCompare(person(y.a),'fr'));const total=ORDER.reduce((s,k)=>s+groups.get(k).length,0),latest=rs.map((r:any)=>r.imported_at).filter(Boolean).sort().at(-1),det=[`📅 ${frDate(date)}`,`↳ Mis à jour le : ${frUpdate(latest)}`,'','•••',''];for(const k of ORDER)det.push(`${SHIFT[k].dot} ${SHIFT[k].label} : ${groups.get(k).length} ${groups.get(k).length===1?'agent':'agents'}`);det.push('',`👥 Total : ${total} ${total===1?'agent':'agents'}`,'','•••','');const non=ORDER.filter(k=>groups.get(k).length);non.forEach((k,idx)=>{det.push(`${SHIFT[k].dot} • ${SHIFT[k].label.toLocaleUpperCase('fr-FR')} • ${SHIFT[k].text}`,'');for(const z of groups.get(k))det.push(`${SHIFT[k].dot} • ${gheText(z.c?.ghe||z.a?.ghe)} • ${person(z.a)}`);if(idx<non.length-1)det.push('','···','')});det.push('','•••','','🕒 Horaires','');for(const k of ORDER)det.push(`${SHIFT[k].dot} ${SHIFT[k].label} : ${SHIFT[k].text}`);det.push('','•••','','☎️ Contacts','');non.forEach((k,idx)=>{for(const z of groups.get(k)){const tel=z.c?.telephone||z.a?.telephone;det.push(`${SHIFT[k].dot} ${gheText(z.c?.ghe||z.a?.ghe)} • ${e164(tel)?phonePretty(tel):'—'}`)}if(idx<non.length-1)det.push('','···','')});ev(l,`stip-team-${team}-${date}@esapin.com`,date,"Esprit d'équipe",det.join('\n'),latest);events++}l.push('END:VCALENDAR');return{ics:l.join('\r\n')+'\r\n',events}}

function missingCoverage(target:any,ranges:any[]){let s=mins(target.start),e=mins(target.end);if(e<=s)e+=1440;const rr=ranges.filter(Boolean).map(r=>{let a=mins(r.start),b=mins(r.end);if(b<=a)b+=1440;return[a,b]}).sort((a,b)=>a[0]-b[0]);let cur=s;const gaps:any[]=[];for(const [a,b] of rr){if(b<=cur||a>=e)continue;if(a>cur)gaps.push([cur,Math.min(a,e)]);cur=Math.max(cur,b);if(cur>=e)break}if(cur<e)gaps.push([cur,e]);const fmt=(m:number)=>`${String(Math.floor((m%1440)/60)).padStart(2,'0')}:${String(m%60).padStart(2,'0')}`;return gaps.map(([a,b])=>`${fmt(a)}–${fmt(b)}`)}

async function formations(){const start=ymd(-60),end=ymd(370),dir=await loadDirectory(),{data:rows,error}=await db.from('formations').select('id,source_key,agent_source_key,intitule,date_debut,date_fin,lieu,statut,updated_at,last_seen_at,horaire').gte('date_fin',start+'T00:00:00Z').lte('date_debut',end+'T23:59:59Z').order('date_debut');if(error)throw error;const groups=new Map();for(const r of rows||[]){const k=sessionKey(r);if(!groups.has(k))groups.set(k,[]);groups.get(k).push(r)}const eventsRows:any[]=[];for(const rs of groups.values()){const f:any=rs[0],sd=parisDate(f.date_debut),ed=parisDate(f.date_fin),range=timeRange(f.horaire)||{start:parisTime(f.date_debut),end:parisTime(f.date_fin),text:`${parisTime(f.date_debut)}–${parisTime(f.date_fin)}`},days:string[]=[];let d=sd,g=0;while(d<=ed&&g++<40){days.push(d);d=next(d)}const places=splitFormationPlaces(f.lieu,days.length);days.forEach((day,i)=>eventsRows.push({day,title:String(f.intitule||'Formation'),range,place:places[i],participants:rs,key:keyNorm(`${f.intitule}-${f.date_debut}-${f.horaire}`).replace(/\s+/g,'-').toLowerCase().slice(0,120)}))}eventsRows.sort((a,b)=>a.day.localeCompare(b.day)||a.range.start.localeCompare(b.range.start));const allIds=dir.agents.map((a:any)=>a.id),plan=new Map(),planByDay=new Map();if(allIds.length&&eventsRows.length){const min=eventsRows[0].day,max=eventsRows.at(-1).day,pp=await planningPaged('date,code,agent_id,equipe,imported_at',min,max,{ids:allIds});for(const p of pp){plan.set(`${p.agent_id}|${p.date}`,p);if(!planByDay.has(p.date))planByDay.set(p.date,[]);planByDay.get(p.date).push(p)}}const byAgentDay=new Map();for(const e of eventsRows)for(const r of e.participants){const k=`${r.agent_source_key}|${e.day}`;if(!byAgentDay.has(k))byAgentDay.set(k,[]);byAgentDay.get(k).push(e)}const l=head('Formations');let events=0;for(const e of eventsRows){const ps=e.participants.slice().sort((x:any,y:any)=>person(dir.agents.find((a:any)=>a.source_key===x.agent_source_key)||{source_key:x.agent_source_key}).localeCompare(person(dir.agents.find((a:any)=>a.source_key===y.agent_source_key)||{source_key:y.agent_source_key}),'fr')),latest=ps.flatMap((r:any)=>[r.updated_at,r.last_seen_at]).filter(Boolean).sort().at(-1),det=[`🎓 ${e.title}`,`📅 ${frDate(e.day)}`,`🕘 ${e.range.text}`,`📍 ${e.place||'Lieu non renseigné'}`,'','•••','',`👥 ${ps.length} ${ps.length===1?'participant':'participants'}`,''],globalWarnings:string[]=[];const participantAgents:any[]=[];for(const r of ps){const ag=dir.agents.find((a:any)=>a.source_key===r.agent_source_key),c=contactFor(ag,dir),pr=ag?plan.get(`${ag.id}|${e.day}`):null,k=norm(pr?.code),work=k&&SHIFT[k]?SHIFT[k]:null;participantAgents.push(ag);det.push(`👨‍⚕️ ${person(ag||{source_key:r.agent_source_key})}`);if(work)det.push(`${work.personal} ${work.label} · ${work.text}`);det.push(...contactLines(ag,c));const w:string[]=[];if(String(r.statut||'').toUpperCase()!=='PLANIFIEE')w.push('Inscription à vérifier');if(!ag)w.push('Agent non retrouvé');else if(!pr)w.push('Planning non retrouvé');else if(!work)w.push(`Non planifié en travail ce jour (${String(pr.code||'—')})`);else if(!covers(work,e.range))w.push('Formation hors de son shift');const same=byAgentDay.get(`${r.agent_source_key}|${e.day}`)||[];if(same.some((o:any)=>o!==e&&overlap(o.range,e.range)))w.push('Conflit avec une autre formation');for(const x of [...new Set(w)])globalWarnings.push(`${person(ag||{source_key:r.agent_source_key})} : ${x}`);const tel=e164(c?.telephone||ag?.telephone);if(!tel)globalWarnings.push(`Téléphone manquant : ${person(ag||{source_key:r.agent_source_key})}`);if(!Number.isFinite(gheNum(c?.ghe||ag?.ghe)))globalWarnings.push(`GHE manquant : ${person(ag||{source_key:r.agent_source_key})}`);det.push('')}if(det.at(-1)==='')det.pop();const dayPlan=planByDay.get(e.day)||[],shiftCounts:any={};for(const k of ORDER)shiftCounts[k]={scheduled:0,mobilised:0,remaining:0};for(const p of dayPlan){const k=norm(p.code);if(k&&SHIFT[k]&&shiftCounts[k])shiftCounts[k].scheduled++}for(const ag of participantAgents.filter(Boolean)){const p=plan.get(`${ag.id}|${e.day}`),k=norm(p?.code);if(k&&shiftCounts[k])shiftCounts[k].mobilised++}for(const k of ORDER)shiftCounts[k].remaining=Math.max(0,shiftCounts[k].scheduled-shiftCounts[k].mobilised);const mobilised=ORDER.reduce((s,k)=>s+shiftCounts[k].mobilised,0),remaining=ORDER.reduce((s,k)=>s+shiftCounts[k].remaining,0);det.push('','•••','',`👥 IMPACT ÉQUIPE · ${e.range.start}–${e.range.end}`,`${mobilised} ${mobilised===1?'agent mobilisé':'agents mobilisés'} en formation`,`Effectif planifié restant : ${remaining}`);const impacted=ORDER.filter(k=>shiftCounts[k].mobilised>0);for(const k of impacted)det.push(`${SHIFT[k].personal} ${SHIFT[k].label} : ${shiftCounts[k].remaining} restant${shiftCounts[k].remaining>1?'s':''} / ${shiftCounts[k].scheduled}`);const low=impacted.slice().sort((a,b)=>shiftCounts[a].remaining-shiftCounts[b].remaining)[0],source=ORDER.filter(k=>k!=='N').sort((a,b)=>shiftCounts[b].remaining-shiftCounts[a].remaining)[0];if(low&&source&&source!==low&&shiftCounts[low].remaining<=2&&shiftCounts[source].remaining>=shiftCounts[low].remaining+4){det.push('',`💡 Piste à examiner : ${SHIFT[source].label} est nettement mieux couvert que ${SHIFT[low].label}. Un rééquilibrage de shift peut être envisagé si les contraintes terrain le permettent.`)}if(!e.place||e.place==='Non renseigné')globalWarnings.unshift('Lieu non renseigné');if(globalWarnings.length){det.push('','•••','','⚠️ À vérifier');for(const w of [...new Set(globalWarnings)])det.push(`• ${w}`)}det.push('','↳ Mis à jour le : '+frUpdate(latest));ev(l,`stip-formations-${e.key}-${e.day}@esapin.com`,e.day,`🎓 ${e.title} · ${ps.length} ${ps.length===1?'participant':'participants'}`,det.join('\n'),latest);events++}l.push('END:VCALENDAR');return{ics:l.join('\r\n')+'\r\n',events}}

async function stagiaires(){const start=ymd(-60),end=ymd(370),dir=await loadDirectory(),{data:rows,error}=await db.from('stagiaires').select('id,nom,prenom,date_debut,date_fin,horaires,referent,updated_at,last_seen_at').gte('date_fin',start).lte('date_debut',end).order('date_debut');if(error)throw error;const expanded=new Map();let minDate='',maxDate='';for(const r of rows||[]){let d=String(r.date_debut||''),ed=String(r.date_fin||r.date_debut||''),g=0;while(d&&d<=ed&&g++<370){if(d>=start&&d<=end){if(!expanded.has(d))expanded.set(d,[]);expanded.get(d).push(r);if(!minDate||d<minDate)minDate=d;if(!maxDate||d>maxDate)maxDate=d}d=next(d)}}const resolved=new Map();for(const rs of expanded.values())for(const r of rs)for(const ref of splitRefs(r.referent)){const k=keyNorm(ref);if(k&&!resolved.has(k))resolved.set(k,resolveRef(ref,dir))}const ids=[...new Set([...resolved.values()].map((z:any)=>z?.a?.id).filter(Boolean))],plan=new Map();if(ids.length&&minDate&&maxDate){const pp=await planningPaged('date,code,agent_id,imported_at',minDate,maxDate,{ids});for(const p of pp)plan.set(`${p.agent_id}|${p.date}`,p)}const l=head('Stagiaires');let events=0;for(const [date,rs0] of [...expanded.entries()].sort((a:any,b:any)=>a[0].localeCompare(b[0]))){const rs:any[]=rs0,latest=rs.flatMap(r=>[r.updated_at,r.last_seen_at]).filter(Boolean).sort().at(-1),det=[`📅 ${frDate(date)}`,`👶 ${rs.length} ${rs.length===1?'stagiaire':'stagiaires'}`,'','•••',''];rs.sort((a,b)=>fullPerson(a.prenom,a.nom).localeCompare(fullPerson(b.prenom,b.nom),'fr'));rs.forEach((r,idx)=>{const st=timeRange(r.horaires),refs=splitRefs(r.referent),rr=refs.map(ref=>{const z=resolved.get(keyNorm(ref)),ag=z?.a,c=z?.c,pr=ag?plan.get(`${ag.id}|${date}`):null,k=norm(pr?.code),range=k&&SHIFT[k]?SHIFT[k]:null;return{raw:ref,ag,c,pr,k,range}}),warnings:string[]=[];det.push(`👶 ${fullPerson(r.prenom,r.nom)}`,`🕘 ${st?.text||String(r.horaires||'Horaire non renseigné')}`,'',refs.length>1?'👥 Référents':'👨‍⚕️ Référent','');if(!refs.length)warnings.push('Référent non renseigné');rr.forEach((z,j)=>{det.push(`👨‍⚕️ ${z.ag||z.c?person(z.ag||z.c):String(z.raw).toLocaleUpperCase('fr-FR')}`);if(z.range)det.push(`${z.range.personal} ${z.range.label} · ${z.range.text}`);det.push(...contactLines(z.ag,z.c));if(!z.ag&&!z.c)warnings.push(`Référent non retrouvé : ${z.raw}`);else if(!z.pr)warnings.push(`Planning non retrouvé : ${person(z.ag||z.c)}`);else if(!z.range)warnings.push(`${person(z.ag||z.c)} non planifié en travail (${String(z.pr.code||'—')})`);if(!e164(z.c?.telephone||z.ag?.telephone))warnings.push(`Téléphone manquant : ${person(z.ag||z.c)}`);if(!Number.isFinite(gheNum(z.c?.ghe||z.ag?.ghe)))warnings.push(`GHE manquant : ${person(z.ag||z.c)}`);if(j<rr.length-1)det.push('')});if(st){const gaps=missingCoverage(st,rr.map(z=>z.range).filter(Boolean));if(gaps.length)warnings.push(`Couverture stagiaire manquante : ${gaps.join(', ')}`)}if(warnings.length){det.push('','⚠️ À vérifier');for(const w of [...new Set(warnings)])det.push(`• ${w}`)}if(idx<rs.length-1)det.push('','•••','')});det.push('','↳ Mis à jour le : '+frUpdate(latest));ev(l,`stip-stagiaires-${date}@esapin.com`,date,`Stagiaires · ${rs.length} ${rs.length===1?'présent':'présents'}`,det.join('\n'),latest);events++}l.push('END:VCALENDAR');return{ics:l.join('\r\n')+'\r\n',events}}

async function feed(t:string){const {data:f}=await db.from('stip_calendar_feeds').select('profile_id,kind,active').eq('token',t).maybeSingle();if(!f?.active)return null;const p=await prof(f.profile_id);if(!p)return null;if(f.kind==='personal')return personal(p.agent);if(String(f.kind||'').startsWith('agent:')){const target=await activeAgentById(String(f.kind).slice(6));if(!target||!canSubscribeAgent(p,target))return null;return personal(target,String(target.id)!==String(p.agent.id))}if(f.kind==='team')return team(p.agent);if(f.kind==='formations'){if(!p.permissions?.responsable)return null;return formations()}if(f.kind==='stagiaires'){if(!p.permissions?.responsable)return null;return stagiaires()}return null}

Deno.serve(async r=>{if(r.method==='OPTIONS')return new Response('ok',{headers:C});try{const u=new URL(r.url);if(r.method==='GET'||r.method==='HEAD'){const z=await feed(u.searchParams.get('token')||'');if(!z)return new Response('Flux désactivé',{status:404,headers:C});return new Response(r.method==='HEAD'?null:z.ics,{headers:{...I,'X-STIP-Events':String(z.events),'Content-Disposition':'inline; filename="stip.ics"'}})}if(r.method==='POST'){const p=await session(r);if(!p)return json({error:'Accès Import au calendrier non autorisé.'},401);const b=await r.json().catch(()=>({})),q=String(b.kind||'personal');let k=q==='team'?'team':q==='formations'?'formations':q==='stagiaires'?'stagiaires':'personal',calendarName='';if(q==='agent'){const sourceKey=String(b.source_key||'').trim(),agentId=String(b.agent_id||'').trim(),target=sourceKey?await activeAgentBySource(sourceKey):agentId?await activeAgentById(agentId):null;if(!target)return json({error:'Agent introuvable.'},404);if(!canSubscribeAgent(p,target))return json({error:'Abonnement à ce planning réservé au responsable ou à l’administrateur.'},403);k=`agent:${target.id}`;calendarName=sharedCalendarName(target)}if((k==='formations'||k==='stagiaires')&&!p.permissions?.responsable)return json({error:'Accès Responsable requis pour ce calendrier.'},403);const t=await ensure(p.id,k),h=`${U}/functions/v1/stip-calendar?token=${t}`;return json({kind:k,calendar_name:calendarName|| (k==='personal'?personalCalendarName(p.agent):k==='team'?"Esprit d'équipe":k==='formations'?'Formations':'Stagiaires'),https_url:h,webcal_url:h.replace(/^https:/,'webcal:')})}return json({error:'Méthode non autorisée.'},405)}catch(e){console.error(e);return json({error:e instanceof Error?e.message:String(e)},500)}})
