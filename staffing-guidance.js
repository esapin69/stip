(()=>{'use strict';
const API='https://yzsrmuxghlengnkyphxj.supabase.co/functions/v1/stip-staffing',STORE='stip_session_v1';
let seq=0,lastKey='';
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
async function call(body){const r=await fetch(API,{method:'POST',headers:{'Content-Type':'application/json','X-STIP-Session':localStorage.getItem(STORE)||''},body:JSON.stringify(body),cache:'no-store'}),j=await r.json().catch(()=>({}));if(!r.ok||j.error)throw Error(j.error||`Erreur ${r.status}`);return j}
function box(){const cards=[...document.querySelectorAll('.cw>.cw-card')];if(cards.length<3)return null;let b=document.querySelector('#cwStaffingGuide');if(!b){b=document.createElement('section');b.id='cwStaffingGuide';b.className='cw-card staffing-guide';cards[2].before(b)}return b}
function fmt(d){return new Date(d+'T12:00:00').toLocaleDateString('fr-FR',{weekday:'short',day:'numeric',month:'short'}).replace('.','')}
async function update(){const root=document.querySelector('.cw');if(!root)return;const dates=[...root.querySelectorAll('[data-cw-date].on')].map(x=>x.dataset.cwDate).filter(Boolean),shift=root.querySelector('[data-cw-shift].on')?.dataset.cwShift||'';const b=box();if(!b)return;if(!dates.length){b.hidden=true;return}const k=dates.join(',')+'|'+shift;if(k===lastKey)return;lastKey=k;b.hidden=false;b.innerHTML='<div class="cw-step"><b>i</b><div><strong>Repère du planning</strong><small>Information issue du planning importé.</small></div></div><div class="staffing-loading">Analyse de la couverture…</div>';const my=++seq;try{const rows=await Promise.all(dates.slice(0,10).map(date=>call(shift?{action:'request_impact',date,desired_code:shift}:{action:'day',date})));if(my!==seq)return;const html=rows.map((x,i)=>{if(!x.available)return`<div class="staffing-line neutral"><strong>${esc(fmt(dates[i]))}</strong><span>Aucune référence d’effectif n’a été importée pour cette date.</span></div>`;if(shift&&x.impact){const l=x.impact.level||'neutral';return`<div class="staffing-line ${esc(l)}"><strong>${esc(fmt(dates[i]))} · ${esc(x.current_shift||'—')} → ${esc(x.desired_shift||shift)}</strong><span>${esc(x.impact.message)}</span></div>`}const bad=x.recommendations||[];return`<div class="staffing-line ${bad.length?'watch':'favorable'}"><strong>${esc(fmt(dates[i]))}</strong><span>${esc(bad[0]?.message||'Couverture conforme aux références du planning.')}</span></div>`}).join('');b.innerHTML='<div class="cw-step"><b>i</b><div><strong>Repère du planning</strong><small>Aide à orienter la demande, jamais une décision automatique.</small></div></div>'+html}catch(e){if(my!==seq)return;b.innerHTML='<div class="cw-step"><b>i</b><div><strong>Repère du planning</strong><small>Analyse momentanément indisponible.</small></div></div>'}}
const st=document.createElement('style');st.textContent='.staffing-guide{border-color:#c9e5e8!important;background:#f8fcfc!important}.staffing-line{padding:9px 10px;border-radius:12px;margin-top:6px;background:#eef4f5}.staffing-line strong,.staffing-line span{display:block}.staffing-line strong{font-size:.69rem}.staffing-line span{margin-top:3px;font-size:.66rem;line-height:1.35;color:#536f78}.staffing-line.risk{background:#fdeaea}.staffing-line.risk span{color:#8b3434}.staffing-line.watch{background:#fff5dd}.staffing-line.watch span{color:#7c5a18}.staffing-line.favorable{background:#e6f6ed}.staffing-line.favorable span{color:#216b48}.staffing-loading{font-size:.68rem;color:#71848a;padding:8px}';document.head.appendChild(st);
new MutationObserver(()=>{if(document.querySelector('.cw'))setTimeout(update,0);else lastKey=''}).observe(document.documentElement,{subtree:true,childList:true,attributes:true,attributeFilter:['class']});document.addEventListener('click',e=>{if(e.target.closest?.('[data-cw-date],[data-cw-shift]'))setTimeout(()=>{lastKey='';update()},20)});})();