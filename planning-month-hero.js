(()=>{'use strict';
const MONTHS=['JANVIER','FÉVRIER','MARS','AVRIL','MAI','JUIN','JUILLET','AOÛT','SEPTEMBRE','OCTOBRE','NOVEMBRE','DÉCEMBRE'],SLUGS=['janvier','fevrier','mars','avril','mai','juin','juillet','aout','septembre','octobre','novembre','decembre'],BASE='https://yzsrmuxghlengnkyphxj.supabase.co/storage/v1/object/public/stip-public-assets/planning/months/';
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));let busy=false;
function enhance(){if(busy)return;const card=document.querySelector('#phContent .ph-month-card'),header=card?.querySelector(':scope>header');if(!card||!header)return;const label=header.querySelector('strong')?.textContent?.trim().toUpperCase()||'',year=header.querySelector('small')?.textContent?.trim()||'',idx=MONTHS.indexOf(label);if(idx<0)return;const prevDisabled=!!header.querySelector('[data-month-nav="-1"]')?.disabled,nextDisabled=!!header.querySelector('[data-month-nav="1"]')?.disabled,boot=window.STIPBootCache||{},a=boot.agent||window.STIPSession?.agent||{},avatars=boot.media?.avatars||{},avatar=avatars[a.source_key]||avatars[a.id]||a.avatar_signed_url||a.avatar_url||'';busy=true;const hero=document.createElement('section');hero.className='ph-month-hero';hero.innerHTML=`<div class="phmh-person"><div class="phmh-year">${esc(year)}</div><div class="phmh-avatar">${avatar?`<img src="${esc(avatar)}" alt="">`:`<span>${esc((a.prenom?.[0]||'')+(a.nom?.[0]||''))}</span>`}</div></div><div class="phmh-carousel" aria-label="Changer de mois"><button class="phmh-arrow prev" type="button" data-month-nav="-1" aria-label="Mois précédent" ${prevDisabled?'disabled':''}><svg viewBox="0 0 48 48" aria-hidden="true"><path d="M29 10L15 24l14 14"/></svg></button><div class="phmh-center"><img src="${BASE}${SLUGS[idx]}.png" alt="${esc(label)}"></div><button class="phmh-arrow next" type="button" data-month-nav="1" aria-label="Mois suivant" ${nextDisabled?'disabled':''}><svg viewBox="0 0 48 48" aria-hidden="true"><path d="M19 10l14 14-14 14"/></svg></button></div>`;header.replaceWith(hero);const carousel=hero.querySelector('.phmh-carousel');let sx=0,sy=0,tracking=false;carousel.addEventListener('touchstart',e=>{const t=e.touches?.[0];if(!t)return;sx=t.clientX;sy=t.clientY;tracking=true},{passive:true});carousel.addEventListener('touchend',e=>{if(!tracking)return;tracking=false;const t=e.changedTouches?.[0];if(!t)return;const dx=t.clientX-sx,dy=t.clientY-sy;if(Math.abs(dx)<34||Math.abs(dx)<=Math.abs(dy))return;hero.querySelector(`[data-month-nav="${dx<0?1:-1}"]:not(:disabled)`)?.click()},{passive:true});busy=false}
function schedule(){setTimeout(enhance,0);requestAnimationFrame(()=>requestAnimationFrame(enhance))}
const obs=new MutationObserver(schedule);function watch(){const c=document.querySelector('#phContent');if(c)obs.observe(c,{childList:true,subtree:false});schedule()}
window.addEventListener('stip:planning-select',e=>{if(e.detail?.kind==='personal')watch()});window.addEventListener('stip:boot-updated',schedule);window.addEventListener('stip:month-changed',()=>setTimeout(schedule,10));setTimeout(watch,80);
})();